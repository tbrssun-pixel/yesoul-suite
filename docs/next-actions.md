<!-- generated_by: start-new-project; refined_by: codex -->

# Next Actions

Generated by `start-new-project` on 2026-06-11 and refined after reviewing the actual Android project layout.

## 1. Run Understand Anything and regenerate the context pack.

- Status: recommended
- Evidence: `.understand-anything/knowledge-graph.json` is missing, so the initial bootstrap used static inspection.

## 2. Keep generated context focused on source and reviewed docs.

- Status: done for this pass
- Evidence: ignored local caches such as `.android-sdk/`, `.build-tools/`, `.gradle/`, `app/build/`, APK/AAB outputs, keystores, and `output/playwright/` screenshots are build/review artifacts, not product modules.

## 3. Use the Android build helper as the baseline verification command.

- Status: ready
- Evidence: `README.md` documents `.\tools\build-debug-apk.ps1`; use `-SkipSdkInstall` when local SDK/Gradle setup already exists. Release bundle task validation uses `.\tools\build-release-aab.ps1 -SkipSdkInstall -UnsignedCheckOnly`.

## 4. Finish Play Console prerequisites before production-style deploy work.

- Status: open
- Evidence: the repo now has a neutral package id and release AAB helper, but Play Console account type, public privacy URL, tester countries, and closed-test roster are still external prerequisites.

## 5. Prepare controlled beta evidence.

- Status: open
- Evidence: v1 success requires at least 12 testers for the stricter personal-account path and at least 3 real YESOUL Bike V1 / YS-003 owners who can submit CSV/TCX export or sanitized diagnostics.

## 6. Keep OpenSpec current-state only.

- Status: ready
- Evidence: `openspec/specs/**/spec.md` documents current v1 behavior for BLE, workouts, export, local privacy, release gates, and race/goal/baseline. Future integrations remain out of scope until a separate change is proposed.

## Handoff Notes

- Start implementation only after reviewing local/public boundaries in `AGENTS.md`.
- Run verification commands from `docs/verification.md` after changes.
- Use `docs/play-compliance.md`, `docs/privacy-policy.md`, and `docs/store-listing.md` before Play Console submission.
- Use `knowledge/rag/extractive/index.html` for quick local lookup.
- Use `openspec/specs/` as the behavior contract for current v1 capabilities.
